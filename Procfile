# Railway Deployment Configuration
# Este archivo ayuda a Railway a entender cómo deployar tu app

# Build commands que Railway ejecutará
web: npm run build && npm run prisma:generate && npm run prisma:migrate && npm start